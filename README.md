# Traffic-Light-Controller-using-Verilog-
The Traffic Light Controller is a Verilog HDL project that implements a finite state machine (FSM) to control Red, Yellow, and Green traffic signals using sequential logic. The design performs automatic state transitions based on predefined timing intervals and initializes to the Red state after reset. 
